<div class="container text-center" id="set-password-container">
    <p id="set-password-logo">REIFY</p>

    <div class="col-md-8 col-md-offset-2" id="set-password-desc">
        <p> An account has been created for you already. Set your password below to view your current orders and account info. Now that your body measurements are on file, you'll be able to shop anywhere online and ensure a guaranteed perfect fit </p>
    </div>

    <div class="panel panel-default col-md-4 col-md-offset-4" id="set-password-form">
        <div class="panel-body">
            <form role="form-horizontal" method="POST" action="/set-password/{{ token }}" >


                <div class="form-group">
                    <p class="form-control-static" >{{ email }}
                        <a href="#" data-toggle="popover" data-trigger="hover" data-content="Not you? Shoot us an email to info@reify.fit and we'll get it sorted out!">
                            <span class="glyphicon glyphicon-question-sign"></span>
                        </a>
                    </p>
                    <input type="text" class="form-control hidden" name="email" id="email" value="{{ email }}">
                </div>
                <div class="form-group">

                    <div >
                        {{#if noPassword }}
                            <input type="password" class="form-control alert-danger" name="password" id="pwd" placeholder="Password cannot be empty">
                        {{ else }}
                            <input type="password" class="form-control" name="password" id="pwd" placeholder="Enter password">
                        {{/if}}
                    </div>
                </div>
                <div class="form-group">
                    <div >
                        <button type="submit" class="btn btn-default btn-block" >Submit</button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>

<script>
    $(document).ready(function(){
        $('[data-toggle="popover"]').popover();
    });
</script>